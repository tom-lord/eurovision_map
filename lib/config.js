// Default config, which can be altered within the app
CONFIG = {
    colour_scheme: COLOUR_SCHEMES.green_to_red,
    // from_country, to_country, final_scores or spearman_ranked
    vote_type: "from_country",
    year: 2014,
    // combined, televote or jury
    voters: "combined",
}
